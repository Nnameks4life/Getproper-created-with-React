import React from "react";

const Button = () => {
  return (
    <div className="d-flex justify-content-right ">
      <button className="btn bg-warning">Get propa!</button>
    </div>
  );
};

export default Button;
